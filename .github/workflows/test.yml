on: [push]
jobs:
  test:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: ../PSGitUtils.Tests
    steps:
      - uses: zyborg/pester-tests-report@v1.4.0
        with:
          github_token: ${{ secrets.GH_TOKEN }}
